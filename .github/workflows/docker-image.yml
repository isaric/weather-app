name: Docker Image CI

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    name: Build and Push the latest Docker image to Docker Hub
    runs-on: ubuntu-latest
    
    steps:
    - name: Build and Push the Docker image
    - uses: MaximilianoBz/dockerhub-buildpush@v1.1
      with:
    # Docker Registry Url, eg: docker.io
        registry_url: docker.io
        repository_name: isaricpv/weather-app
        # Docker Login UserName, from: https://hub.docker.com/
        user_name: isaricpv
        # Docker Login Access Token, from: https://hub.docker.com/
        password: ${DOCKER_PAT}
        # Image Build Version
        image_version: latest
